# Backend environment example

# Postgres connection
DATABASE_URL=postgresql://user:password@localhost:5432/churchdb?schema=public

# JWT secret (development only)
JWT_SECRET=dev_jwt_secret

# Dev seed admin credentials (development only)
RBAC_DEV_ADMIN_EMAIL=admin@example.com
RBAC_DEV_ADMIN_PASSWORD=admin
